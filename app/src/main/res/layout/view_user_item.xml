<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:background="@color/blackWithAlpha"
                android:orientation="horizontal">

  <ImageView
      android:id="@+id/user_view_image"
      android:layout_width="120dp"
      android:layout_height="120dp"
      android:contentDescription="@string/cd_user_details_image" />

  <TextView
      android:id="@+id/user_view_name"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_centerVertical="true"
      android:layout_marginEnd="56dp"
      android:layout_marginRight="56dp"
      android:layout_toEndOf="@+id/user_view_image"
      android:layout_toRightOf="@+id/user_view_image"
      android:textColor="@color/colorPrimaryDark"
      android:textSize="22sp"
      tools:text="Joao Paulo" />

</RelativeLayout>
